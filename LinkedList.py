class Node:
    def __init__(self):
        self.data = data
        self.next = None
    
    def insert(self,data);
        new_node = Node(data)
        if self.data is None:
            self.head = new_node
            return
        else:
            new_node.next = self.head
            self_head = new_node

